Run git status and git diff to see all changes, then write a concise commit message that describes what changed and why. Stage the relevant files and create the commit. Then ask the user if they want to push before pushing.

Also update DEVLOG.md with a new entry for this commit — what was built, any decisions made, and current project structure if it changed.
